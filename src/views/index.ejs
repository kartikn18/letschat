<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Join Chat</title>

<style>
* { margin: 0; padding: 0; box-sizing: border-box; }

body {
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, sans-serif;
  background: radial-gradient(circle at top, #1a1a2e, #000);
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 20px;
  color: #fff;
}

/* Header title */
h1 {
  margin-top: 15px;
  margin-bottom: 10px;
  font-size: 32px;
  color: #b69cff;
  letter-spacing: 1px;
  text-align: center;
}

/* Tagline bar */
.tagline-bar {
  width: 100%;
  max-width: 460px;
  text-align: center;
  padding: 10px 15px;
  margin-bottom: 25px;
  background: linear-gradient(
    135deg,
    rgba(182,156,255,0.18),
    rgba(138,108,255,0.05)
  );
  border-radius: 14px;
  border: 1px solid rgba(255,255,255,0.15);
  backdrop-filter: blur(10px);
  box-shadow: 0 0 25px rgba(182,156,255,0.25);
}

.tagline-text {
  color: #e0d7ff;
  font-size: 13px;
  letter-spacing: 0.3px;
  text-shadow: 0 0 10px rgba(182,156,255,0.5);
  animation: glowPulse 3s ease-in-out infinite;
}

@keyframes glowPulse {
  0% { text-shadow: 0 0 6px rgba(182,156,255,0.4); }
  50% { text-shadow: 0 0 14px rgba(182,156,255,0.9); }
  100% { text-shadow: 0 0 6px rgba(182,156,255,0.4); }
}

/* Card form */
form {
  background: rgba(0,0,0,0.55);
  backdrop-filter: blur(14px);
  border-radius: 20px;
  box-shadow: 0 25px 60px rgba(0,0,0,0.8);
  width: 100%;
  max-width: 420px;
  padding: 35px 30px 40px 30px;
  border: 1px solid rgba(255,255,255,0.15);
  display: flex;
  flex-direction: column;
  gap: 20px;
}

/* Inputs */
label { font-weight: 500; color: #ddd; }

input {
  width: 100%;
  padding: 14px 16px;
  border-radius: 12px;
  border: 1px solid rgba(255,255,255,0.2);
  background: rgba(255,255,255,0.08);
  color: #fff;
}

input::placeholder { color: #aaa; }

input:focus {
  outline: none;
  border-color: #b69cff;
  box-shadow: 0 0 0 3px rgba(182,156,255,0.3);
}

/* Button */
button {
  width: 100%;
  padding: 16px;
  background: linear-gradient(135deg, #b69cff, #8a6cff);
  color: #fff;
  border: none;
  border-radius: 12px;
  font-size: 16px;
  font-weight: 600;
  cursor: pointer;
  box-shadow: 0 6px 20px rgba(138,108,255,0.5);
}

button:hover {
  transform: translateY(-2px);
  box-shadow: 0 10px 30px rgba(138,108,255,0.8);
}

/* Error */
.error {
  max-width: 420px;
  background: rgba(255,60,60,0.15);
  color: #ff7b7b;
  border: 1px solid rgba(255,60,60,0.3);
  padding: 12px;
  border-radius: 10px;
  text-align: center;
  margin-bottom: 10px;
}

/* Tip */
p.tip {
  color: #aaa;
  font-size: 13px;
  margin-top: 20px;
  text-align: center;
  line-height: 1.5;
}

p.tip strong { color: #b69cff; }
</style>
</head>

<body>

<h1>Join Chat</h1>

<div class="tagline-bar">
  <span class="tagline-text">
    ✨ Join rooms, create your own, and chat instantly
  </span>
</div>

<% if (error) { %>
  <div class="error">⚠️ <%= error %></div>
<% } %>

<form id="joinForm" action="/join" method="post">
  <label>Username</label>
  <input name="username" required placeholder="Enter your username" />

  <label>Room Name</label>
  <input name="room" required placeholder="Enter room name" />

  <label>Room Password</label>
  <input type="password" name="password" required placeholder="Enter room password" />

  <button type="submit">Join Chat</button>
</form>

<p class="tip">
  💡 <strong>Tip:</strong> If the room doesn't exist, it will be created with your password. Others will need this password to join.
</p>

<script>
const form = document.getElementById('joinForm');
const btn = form.querySelector('button');

form.addEventListener('submit', () => {
  btn.disabled = true;
  btn.textContent = "Joining...";
  btn.style.opacity = 0.8;
});
</script>

</body>
</html>
